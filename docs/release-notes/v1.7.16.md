# v1.7.16：侧栏布局与额度悬停提示

- 合并 PR #112：紧凑两列中的额度窗口恢复单行，长标签省略，保留进度条和百分比。
- 合并并补齐 PR #110：MiniMax、Codex、通用 Coding Plan 和网关显示完整窗口名、百分比方向、重置时间及倒计时；悬停或键盘聚焦时重新计算，支持插件中英文配置。
- 处理无效/已到期重置时间，修正多账号网关来源前缀与剩余方向文案。
- 构建脚本支持任意工作目录，移除个人电脑的固定依赖路径。

验证：Asia/Shanghai、UTC 全量回归通过；240px、300px 侧栏的标准/紧凑布局使用合成窗口数据进行浏览器检查。宿主兼容声明沿用 1.7.15 的独立验证记录，本次未新增宿主版本声明。

## 升级

```powershell
irm https://raw.githubusercontent.com/Han-1413141/dsh-cost-meter/v1.7.16/install.ps1 | iex
```

升级后重启 dsh web，并刷新浏览器页面。
